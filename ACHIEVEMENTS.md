# 首页、结算页与成就系统

后续更新：首页的新局入口已升级为按种子创建/恢复，见 [种子与留档说明](SEEDS.md)。主游戏现有进程锁防止同目录双开。

运行 `src/gameBody.java`。首页显示英文地下城主题标题；有存档时提供继续、新游戏、成就、退出，无存档时提供开始、成就、退出。成就图鉴按 Enter 返回首页。

结算区分胜利、失败和输入关闭时的暂停，展示角色、楼层、击杀、伤害、金币、评分、持有遗物以及本次新解锁成就。评分公式、战斗数值、Creator 隐藏选择规则不变。

## 成就

| 名称 | 条件 |
| --- | --- |
| First Blood | 击败任意敌人 |
| Elite Hunter | 击败精英 |
| Guardian Breaker | 击败 Boss |
| Deep Delver | 到达第 5 层 |
| Relic Keeper | 单局同时持有 5 件不同遗物 |
| Gold Seeker | 单局累计获得 300 金币 |
| Battle Veteran | 单局累计造成 1,000 点实际生命伤害 |
| Twin Cores | 在主游戏冒险中赢得推格子试炼 |
| Star Bearer | 通关 |
| Nightmare Conqueror | Nightmare 或 Ultra Nightmare 通关 |
| Abyss Master | Ultra Nightmare 通关 |

成就不奖励属性、不影响难度，Creator 也可以解锁。独立推格子测试入口不写入正式成就。
解锁在检查点、试炼胜利和结算处检查；不会把反复读取同一存档的统计相加。
旧存档恢复时可以补解锁其统计已满足的成就，但不能还原没有记录的历史试炼或历史通关。

保存位置为 `saves/achievements.properties`，与 `abyss-expedition.save` 分开；使用 `abyss.saveDir` 时跟随该目录。死亡、通关或新建冒险不会清除成就。
写入采用临时文件替换；读取失败保留原文件，并明确提示当前会话只记录内存进度。写入失败时提示，之后再次检查时重试。
只保证单进程使用；不要同时开启多个游戏实例写同一个目录。

验证：JDK 17 目标编译通过，160 项回归断言通过；自动化覆盖首页导航/退出、存档不变、成就阈值、跨实例读取、去重、损坏档保护、通关删除冒险档后成就保留、结算新成就展示及 KZ 隐藏选择。
