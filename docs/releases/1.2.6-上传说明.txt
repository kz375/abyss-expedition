1.2.6 护盾动画与检查修复更新包

适用于已上传 1.2.4 或 1.2.5 融合内容的仓库。

解压后，将 src 和 web 文件夹拖到 GitHub 仓库根目录的上传页面，保留目录结构，提交更改。
然后到 Render 部署最新提交，部署成功后刷新网页，界面应显示 1.2.6。
这里的 Markdown 检查报告、TXT 说明不用上传；ZIP 本身也不用上传。

本次 src 与 web 都有改动，需要一起替换。
本次 styles.css 已清除旧的固定页面/过高战斗窗补丁，应和 app.js、shield-bars.css、index.html 一起更新。
不需要覆盖整个仓库，也不要先删除服务器存档。

检查报告中区分了已修复问题和仍待设计的平衡/兼容事项。本次没有替你部署到公网。
